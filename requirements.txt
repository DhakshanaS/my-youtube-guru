# My YouTube Guru — pinned for reproducible installs (Python 3.11+)
# Versions verified against PyPI on 2026-07-17.

# ── Web framework / API layer ────────────────────────────────
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32      # multipart/form-data support for the Takeout .zip upload

# ── Typed models & configuration ─────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.2     # 12-factor settings from environment / .env

# ── RAG stack ────────────────────────────────────────────────
sentence-transformers==5.6.0  # all-MiniLM-L6-v2 embeddings (pulls in torch)
chromadb==1.5.9               # persistent local vector database
youtube-transcript-api==1.2.4 # lazy transcript fetching

# ── LLM client (DeepSeek exposes an OpenAI-compatible API) ───
openai==2.46.0

# ── Takeout parsing ──────────────────────────────────────────
lxml==6.1.1                   # fast C-based HTML parser for watch-history.html
